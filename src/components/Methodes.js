import React, {Component} from 'react';
import { Container, Text} from 'native-base'

export default class Methodes extends Component {
  render() {
    return (
      <Container>

      </Container>
    );
  }
}